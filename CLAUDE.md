project-workflow v1.0 기준으로 초기화됨 (2026-09-06)

# 이 프로젝트에서 지킬 것

- 현재 버전 문서는 `docs/current/`에 있다. 5종은 사람이 쓴다. **고치지 않는다**
- `backlog.json`은 CLI로만 바꾼다. 직접 편집하지 않는다
- 진행 중에 task를 추가하지 않는다. 계획 밖 작업은 `docs/current/PROGRESS.md`에 적는다
- task를 닫을 때마다 `PROGRESS.md`에 무엇을·결과·검증을 남긴다
- `docs/history/` 아래는 읽기만 한다. 수정도 삭제도 하지 않는다
- 완료 조건은 `PLAN.md`·`backlog.json`에 적힌 것으로 판정한다. 스스로 정하지 않는다
- 요구가 바뀌면 `SPEC.md`부터 고친다. 코드나 backlog로 우회하지 않는다
- 되돌릴 수 없는 작업(배포·삭제·외부 상태 변경)은 먼저 묻는다

# 이 코드에서 지킬 것

버전이 올라가도 계속 유효한 제약이다. 왜 그렇게 정했는지는 괄호 안 결정 번호로
`docs/history/`나 `docs/current/DECISIONS.md`에서 찾는다.

- 두 갈래(pywebview·Electron)는 같은 계약과 공통 디자인 파일을 공유한다. 한쪽만 고치지 않는다 (v0.1 D-15·D-17·D-18, D-8)
- 색·글꼴·간격은 `shared/design` 토큰으로만 정의하고, 값과 격자 정렬 규칙을 지킨다 (v0.1 D-7·D-8, D-10)
- 아이콘은 단색 테두리 한 벌로 통일하고 채움은 켜짐에만 쓴다. 그림문자를 쓰지 않는다 (v0.1 D-9, D-10)
- Shell 코드는 실행 환경의 이름과 API를 알지 못하며, `shell/`은 `host_*` 밖에 둔다 (v0.1 D-20, D-12)
- 껍데기는 리소스 종류를 모른다. 종류를 아는 코드는 갈아끼우는 자리 안에만 둔다 (D-3, FR-4)
- 빌드 단계를 넣지 않는다. 번들러·트랜스파일러·프레임워크 없이 두 호스트가 소재를 그대로 로드한다 (D-13, 제약 3)
- 도메인 로직과 코드 생성기를 넣지 않는다. 복제 단위는 프로젝트 전체다 (v0.1 D-1·D-16·D-22, BRIEF 3절)
- 보존하는 것은 Shell의 화면 상태뿐이고, 실행 파일 옆 한 파일에 담는다 (v0.1 D-19, D-5)